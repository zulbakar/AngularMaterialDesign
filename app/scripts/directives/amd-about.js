'use strict';

/**
 * @ngdoc directive
 * @name angularMaterialDesignApp.directive:amdAbout
 * @description
 * # amdAbout
 */
angular.module('angularMaterialDesignApp')
  .component('amdAbout', {
    templateUrl: 'views/amd-about.html',
    controller: ['$log',
      function amdAbout($log) {
        var $ctrl = this;


      }]
  });

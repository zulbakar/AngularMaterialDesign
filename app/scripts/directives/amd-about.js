'use strict';

/**
 * @ngdoc directive
 * @name angularMaterialBoilerplateApp.directive:amdAbout
 * @description
 * # amdAbout
 */
angular.module('angularMaterialBoilerplateApp')
  .component('amdAbout', {
    templateUrl: 'views/amd-about.html',
    controller: ['$log',
      function amdAbout($log) {
        var $ctrl = this;


      }]
  });

'use strict';

/**
 * @ngdoc directive
 * @name angularMaterialDesignApp.directive:amdForm
 * @description
 * # amdForm
 */
angular.module('angularMaterialDesignApp')
  .component('amdForm', {
    templateUrl: 'views/amd-form.html',
    controller: ['$log',
      function amdForm($log) {
        var $ctrl = this;


      }]
  });
